@{
    ViewData["Title"] = "Truy c?p b? t? ch?i";
}

<div class="container text-center" style="padding: 60px 15px;">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="error-template">
                <h1 style="font-size: 120px; font-weight: bold; color: #ffc107;">
                    403
                </h1>
                <h2 style="margin-bottom: 30px;">
                    Truy c?p b? t? ch?i
                </h2>
                <div class="error-details" style="margin-bottom: 30px; font-size: 18px; color: #6c757d;">
                    B?n không có quy?n truy c?p vào trang này. Vui lòng liên h? qu?n tr? viên n?u b?n cho r?ng ?ây là l?i.
                </div>
                <div class="error-actions">
                    <a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-lg" style="margin-right: 10px;">
                        <i class="fas fa-home"></i> V? trang ch?
                    </a>
                    <a asp-controller="Account" asp-action="Login" class="btn btn-outline-primary btn-lg">
                        <i class="fas fa-sign-in-alt"></i> ??ng nh?p
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

@if (!string.IsNullOrEmpty(ViewData["RequestId"]?.ToString()))
{
    <div class="text-center" style="margin-top: 30px;">
        <small class="text-muted">ID yêu c?u: @ViewData["RequestId"]</small>
    </div>
}